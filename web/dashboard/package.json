{
  "name": "doppio-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser demo for the doppio compiled-ledger format. Validates the format-as-API claim end-to-end via a JS-native .dop reader.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "fixtures": "cd ../.. && cargo run -q --bin dop -- compile web/dashboard/fixtures/sample.ledger -o web/dashboard/public/sample.dop"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "doppio-dop": "*",
    "pinia": "^2.2.0",
    "vue": "^3.5.0",
    "vue-chartjs": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@vitejs/plugin-vue": "^5.1.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.9",
    "vue-tsc": "^2.1.0"
  }
}
